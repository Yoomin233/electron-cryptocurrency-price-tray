{
  "name": "Coins-Price-Tracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "cd ./assets && source ./CreateIcns.src && cd ../ &&NODE_ENV=production electron-packager . --platform=darwin --arch=x64 --out=out --icon=./assets/MyIcon.icns --overwrite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron-packager": "^16.0.0",
    "electron-prebuilt": "^1.2.7"
  },
  "dependencies": {
    "pako": "^1.0.6"
  }
}
